import type { Metadata } from "next";
import PageContent from "./PageContent";

export const metadata: Metadata = {
  title: "Form State Example",
};

export default function FormStateExample() {
  return <PageContent />;
}
