import React from "react";
import PageContent from "./PageContent";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Use Hooks for Context",
};

export default function PageContextDemo() {
  return <PageContent />;
}
