import type { Metadata } from "next";
import PageContent from "./PageContent";

export const metadata: Metadata = {
  title: "Use Hooks for resources",
};

export default function Resources() {
  return <PageContent />;
}
